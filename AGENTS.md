# 项目协作约束

本文件定义所有在本仓库工作的 AI Agent 必须遵守的项目级约定。

## 规划、设计与执行

### 1. 文档语言与质量

凡是需要用户审阅的需求文档、设计文档和执行计划，正文必须使用中文；代码、命令、路径、接口名、专有名词和必要的原文引用可保留原语言。

#### Karpathy 设计门禁

编写或修订设计文档时，必须在本轮首次修改前完整读取并使用 `karpathy-guidelines`。工作计划或待办中必须包含独立的“Karpathy 精简与自检”步骤；不得将其合并到格式检查、链接检查或通用验证中。

“使用 `karpathy-guidelines`”以实际设计结果和下列证据为准，不以读取 Skill、声明已使用或调用成功为准：

1. **基线对照：** 修改前读取权威需求、相关设计和相关源码；列出会影响方案的既有接口、行为和限制。
2. **假设与取舍：** 明确记录假设、不确定项和必要取舍；存在两种合理解释且会导致不同行为时，必须询问用户或明确列为待批准差异。
3. **最小方案：** 至少比较当前方案与一个更简单方案，说明更简单方案为何不能满足核心目标；不得增加核心目标不需要的能力、抽象、配置项或扩展点。
4. **删除审计：** 逐节检查每项内容能否直接映射到需求、设计决策或验证；删除重复、无关和仅用于显得完整的内容。精简不得删除、弱化或推迟既有要求。
5. **直接验收：** 成功标准必须验证用户可观察的核心结果；文件存在、标题匹配、测试通过、`git diff --check` 等过程证据不得代替核心行为验收。

在把文档标记为“待评审”、提交文档或请求用户批准前，交付说明必须报告：

- 本次确认的假设、不确定项和取舍；
- 采用的最小方案以及拒绝更简单方案的原因；
- 删除或合并了哪些重复、无关或过度设计内容；
- 哪些成功标准直接证明核心目标；
- 是否存在未经用户批准的行为差异。

只报告“已使用 `karpathy-guidelines`”，或只通过格式、链接、占位符、行数和 `git diff --check` 检查，不视为通过本门禁。任一证据缺失时，文档仍为草稿，不得提交、请求批准或进入执行计划。

如果 `karpathy-guidelines` 不可用或无法完整读取，必须停止设计并向用户报告，不得凭记忆或自行概括代替。

### 2. 文档归属与命名

本项目所有需求文档和设计文档统一放在项目根目录 `specs/`，使用 Markdown 格式，不得写入其他目录。

文档分别命名为：

- `YYYY-MM-DD-<topic>-requirements.md`
- `YYYY-MM-DD-<topic>-design.md`（或同日期前缀的 implementation 等后缀）

并遵循 `specs/` 下现有规格文档的统一格式：中文标题、文档版本、文档状态、创建/修订日期、适用范围等元数据，以及编号章节。

无法确定所属范围或命名时，必须先询问用户。除保留的 `plans/` 外，不得在 `specs/` 下创建其他目录层级。除非用户明确要求，不主动迁移或重命名既有文档。创建或修改规格后，检查其位于 `specs/` 且没有在其他目录留下副本。

### 3. 执行计划

执行计划放在 `specs/plans/<topic>/` 下，其中 `<topic>` 与需求或设计文档中的名称保持一致。子目录以 `README.md` 作为实施路线图，说明阶段目标、执行顺序、依赖关系和整体完成标准。

任务需要两个或以上具有先后依赖、可独立审阅或验收的阶段时，每个阶段使用独立的 `YYYY-MM-DD-NN-<step-title>.md`；单阶段任务同样按此命名，不使用 `plan.md` 之类的非编号文件名。

如果外部 Skill、插件或通用工作流给出的默认 spec/plan 路径与本文规则冲突，始终以本文件规定的项目路径和现有格式为准。

无法确定任务是否需要分阶段时，必须先询问用户。

### 4. 人工评审门禁

相关需求文档和设计文档获得用户明确批准后，才能编写执行计划；执行计划获得用户明确批准后，才能创建或修改实现代码。

用户未回复、仅查看文档、批准其他阶段或此前的一般性同意，均不视为当前阶段已批准。不得以任务简单、工期紧张或方案已经明确为由跳过人工评审。

## 核心目标守恒

开始任务前明确核心目标、用户硬约束和直接验收证据。核心目标是用户最终可观察结果，不是文件、计划、测试或提交等过程状态。

用户明确的指令、规则、流程、模板、接口和既有行为均为可执行要求，不得自由概括。实施中可以调整组织形式，但不得擅自改变用户要求的范围、顺序、强度、粒度、行为或完成标准。存在两种合理解释且会造成行为差异，或需要缩小、替代、推迟要求时，先说明影响并获得用户批准。

用户纠正方向后，最新要求优先于此前批准、设计和计划；停止冲突的旧框架，按最新要求重新检查目标、约束、实现与验证，并清理冲突或弱化内容。

验证必须直接覆盖核心结果和硬约束；测试通过、文件存在、结构匹配、`git diff --check` 或提交完成不能代替行为验收。声明完成前，确认核心结果已经出现、硬约束均已满足，且不存在未经批准的缺失、弱化或行为差异；否则只能报告进展与缺口。

## Prompt 评审

默认 Prompt 面向主流大模型，是自然语言行为指令；除非用户明确要求，不按程序代码或形式化规范评审。

1. **先定义预期行为：** 明确目标模型、使用场景、核心目标和必须保留的行为；信息不足时说明假设，不静默猜测。
2. **评审行为而非字面：** 重点检查适用范围、条件、优先级、冲突处理、执行顺序和完成标准；改写或压缩时，对照原文确认这些行为没有改变。
3. **问题必须有实际影响：** 只有存在两种合理理解，并能用具体场景证明它们会导致不同决策时，才判定为实质问题；否则仅作为可选建议。
4. **证据与风险匹配：** 通常采用静态语义分析和最少量的行为试跑；只有用户明确要求或风险确实较高时，才增加形式化评测。
5. **坚持最小必要修改：** 只删除不影响模型决策的内容，只修复已确认的问题，不扩展范围、不重写无关部分，也不为假想场景增加规则。

评审通过的条件是：目标模型明确知道何时做什么，关键行为与原意一致，且不存在可能改变实际决策的合理歧义。

## Skill 调用约束

严禁任何大模型或 AI Agent 未经用户明确要求，自主调用 `superpowers` 相关 Skill（包括 superpowers 插件提供的 brainstorming、writing-plans、executing-plans、subagent-driven-development 等全部 Skill）。

- 只有用户在当前对话中明确提出调用 superpowers 相关 Skill 时，才可以调用。
- 此前对话中的同意、对其他工具或 Skill 的授权、任务特征与某个 Skill 匹配，均不构成本项授权。
- 即使认为 superpowers 流程有助于提高质量，也必须先向用户说明并等待明确同意，不得以“流程需要”为由自主调用或加载。
- 若 superpowers 相关 Skill 被自动触发或加载，应立即停止执行并向用户报告。

## 变更检查

- 提交前运行 `git diff --check`，并检查规格与计划中的相对链接有效。

## Agent skills

### Issue tracker

规格与执行 tickets 使用 `specs/` 原生本地 Markdown 工作流。涉及发布、读取或更新规格与 tickets 时，读取 `docs/agents/issue-tracker.md`。

### Triage labels

使用默认五类 triage 标签，并与文档人工评审状态分开记录。涉及分诊或 Agent 执行状态时，读取 `docs/agents/triage-labels.md`。

## 知识库维护

知识库根目录是 `sutra-pavilion/`，其一级主题目录是子知识库；知识正文和附件必须归属一个子知识库，根目录不存放知识正文。

1. **目标范围：** 维护任务开始写入前必须声明本次目标子知识库，默认一个；确有跨库更新时明确列出全部目标。
2. **脏工作区：** 目标路径已有未提交修改时，停止自动写入并请人工维护者介入；非目标路径的修改不阻塞任务，也不得被清理、格式化、暂存或覆盖。
3. **来源与日期：** 每篇知识文档开头使用引用块记录可识别来源和正文最后一次实质更新日期；索引豁免。直接引文、精确数据和冲突结论在正文相应位置标明具体来源；可靠来源的分歧影响核心结论时，请人工维护者决定。
4. **链接与附件：** 内部链接使用标准 Markdown 相对路径，并验证本次新增或修改的内部链接；附件必须留在所属子知识库内。
5. **路径变更：** 移动、重命名或删除文档前，搜索整个知识库对旧路径的引用，修复全部已知链接后才能提交。存在已知失效内部链接时不得自动提交。
6. **过期判断：** 内容是否过期由人工维护者决定；维护任务只报告疑似过期证据，不得仅凭日期、链接失效或新检索结果自行删除或替换结论。
7. **提交范围：** 自动 commit 前只暂存已声明范围（创建、删除或重命名子知识库时含知识库根 `README.md`），并核对暂存清单没有范围外文件；commit 数量和信息格式不作强制规定。
8. **外部 Skill：** 只有实际进入维护流程时才追加相应规则，不预建注册表、适配层或预留扩展点。
